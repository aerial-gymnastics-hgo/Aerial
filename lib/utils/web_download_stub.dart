import 'dart:typed_data';

void triggerWebDownload(String filename, Uint8List bytes,
    [String mimeType = 'application/octet-stream']) {
  throw UnsupportedError('triggerWebDownload no está disponible en esta plataforma');
}
